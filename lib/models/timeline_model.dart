class TimelineModel {
  String username;
  String profileImage;
  String posttime;
  String likes;
  String content;
  String image;

  TimelineModel(this.username, this.profileImage, this.posttime, this.likes, this.content, this.image);
}